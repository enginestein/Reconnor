# RPC Enumerator

RPC enumerator: endpoint mapper dump, service discovery, unusual port detection.

```
python3 main.py rpc 192.168.1.1
```

**Options:**
- `--target` — Target host
- `--host` — Host alias
- `--port` — RPC port (default: 111)
- `--timeout` — Socket timeout (default: 10)
