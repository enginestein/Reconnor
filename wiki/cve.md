# CVE Search

Searches for known vulnerabilities (CVEs) matching a keyword.

```
python3 main.py cve log4j
python3 main.py cve wordpress --limit 50
```

**Options:**
- `--limit` — Max results (default: 20)
