# Deep Search

Cross-engine deep internet search: file types, code repositories, people search, dork generation.

```
python3 main.py deep-search "company name + credentials"
```

**Options:**
- `--timeout` — HTTP timeout (default: 10)
