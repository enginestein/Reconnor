# Phone Social Media Finder

Finds social media and messaging accounts linked to a phone number.

```
python3 main.py phone-social "+14155551234"
```

**Options:**
- `--timeout` — HTTP timeout (default: 10)
