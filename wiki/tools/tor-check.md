# Tor Check

Tor/dark web reconnaissance: .onion mirrors, exit node detection, dark web search.

```
python3 main.py tor-check example.com
```

**Options:**
- `--timeout` — HTTP timeout (default: 10)
