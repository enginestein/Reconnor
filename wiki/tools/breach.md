# Breach Checker

Checks email/password/username against known data breaches.

```
python3 main.py breach email@example.com
python3 main.py breach mypassword --type password
```

**Options:**
- `--type` — Data type: `email` (default), `password`, or `username`
