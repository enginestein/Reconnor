# Paste Watch

Pastebin and code snippet monitoring: paste search, leak detection, recent monitoring.

```
python3 main.py pastewatch email@example.com
python3 main.py pastewatch "company name + credentials"
```

**Options:**
- `--timeout` — HTTP timeout (default: 10)
