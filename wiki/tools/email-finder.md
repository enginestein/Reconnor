# Email Finder

Finds email addresses from a domain using scraping, pattern guessing, and search engine queries.

```
python3 main.py email-finder example.com
```

**Options:**
- `--timeout` — HTTP timeout (default: 10)
