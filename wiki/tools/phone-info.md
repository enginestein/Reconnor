# Phone Number Intelligence

Phone number information: country, carrier, line type, location, reputation.

```
python3 main.py phone-info "+14155551234"
```

**Options:**
- `--timeout` — HTTP timeout (default: 10)
