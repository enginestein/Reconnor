# SSL/TLS Checker

Inspects SSL/TLS certificate details: issuer, validity, SANs, cipher info.

```
python3 main.py ssl example.com
python3 main.py ssl example.com --port 8443
```

**Options:**
- `--port` — Port number (default: 443)
